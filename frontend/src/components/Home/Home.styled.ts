import styled from "styled-components";
import tw from "twin.macro"

export const HomeContainer = styled.div`
  ${tw`p-7 pt-4 flex flex-col h-[92vh] justify-between` }
`

export const Wrapper = styled.div`
  ${tw`flex h-full gap-5`}
`